def foo(x y) x+foo(y, 4.0);
def foo(x y) x+y y; # considera il secondo y come una seconda top-level expression a parte
def foo(x y) x+y );
extern sin(a);

if x < 3 then
    1
else
    2+x;

def printstar(n)
  for i = 1, i < n, 1.0 in
    putchard(42);  # ascii 42 = '*'

# print 100 '*' characters
printstar(100);

# Binary "logical or", (note that it does not "short circuit")
def binary| 5 (LHS RHS)
  if LHS then
    1
  else if RHS then
    1
  else
    0;
