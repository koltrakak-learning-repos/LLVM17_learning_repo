4+5;

def foo(a b)
    a*a + 2*a*b + b*b;

def bar(a)
    foo(a, 4.0) + bar(31337);

extern cos(x);
cos(1.234);

# error cases
cos(1, 2); # wrong number of arguments
sin(1.234); # call to undeclared function
a+2; # undefined variable
